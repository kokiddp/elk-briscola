{
  "_comment": "Profile used by the Phase 11.5 stress harness. Launch with `--environment Stress`. Relaxes the auth rate limits so a single-host stress runner can register / login many users.",
  "RateLimits": {
    "auth-register": { "PermitLimit": 1000000, "WindowSeconds": 60 },
    "auth-login": { "PermitLimit": 1000000, "WindowSeconds": 60 },
    "auth-refresh": { "PermitLimit": 1000000, "WindowSeconds": 60 },
    "auth-change-password": { "PermitLimit": 1000000, "WindowSeconds": 60 },
    "auth-logout": { "PermitLimit": 1000000, "WindowSeconds": 60 }
  },
  "Game": {
    "ReconnectGraceSeconds": 5,
    "IdleWarnSeconds": 10,
    "IdleForfeitSeconds": 20
  }
}
